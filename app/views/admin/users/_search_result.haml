.hr-line-dashed
.search-result
  - if @user.present?
    -# TODO: logic for a. 已注册或已联系 b. 我来联系
    = @user.inspect
    -# TODO: add real logic & method
    -# - if @user.registered?
    -#   已注册
    -# - elsif @user.contacted?
    -#   已联系
    -# - else
    -#   = link_to '我来联系', '#'
  - else
    此手机号码不存在
    -# b. 我来联系?
