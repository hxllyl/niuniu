<div role="tabpanel" class="panel panel-default contentBox">
	<ul role="tablist" class="nav nav-tabs carInfoTabs">
		<li role="presentation" class="active"><a href="#allCarInfo"><%= t('post_info') %></a></li>
    <% @hot_brands[0..3].each_with_index do |brand, i| %>
		  <li role="presentation"><a href="#carBrand<%= i + 1%>"><span class="icon"><%= image_tag(brand.car_photo.try(:image_path), alt: "")%></span><%= brand.name %></a></li>
	  <% end-%>
  </ul>

	<div class="panel-body">
		<div role="tabpanel" class="tab-pane active" id="allCarInfo">
			<ul class="carInfoList clearfix">
        <%- @needs.each do |post| %>
				<li>
					<div class="logo bfL"><%= image_tag(post.brand.car_photo.try(:image_path), alt: "") %></div>
					<div class="info">
						<h5 class="tt"><%= link_to post_name(post), post_path(post, {_type: 1}) %></a></h5>
						<p class="clearfix"><span class="bfL"><%= post.color %></span><span class="bfR"><%= post.owner_detail %></span></p>
					</div>
				</li>
				<% end -%>
			</ul>
		</div>


		<div role="tabpanel" class="tab-pane" id="carBrand1">
      <ul class="carInfoList clearfix">
      <% @posts_with_brands.values[0].each do |post| %>
  			<li>
  				<div class="logo bfL"><%= image_tag(post.brand.car_photo.try(:image_path), alt: "") %></div>
  				<div class="info">
  					<h5 class="tt"><%= link_to post_name(post), post_path(post, {_type: 1}) %></h5>
  					<p class="clearfix"><span class="bfL"></span><%= post.color %><span class="bfR"><%= post.owner_detail %></span></p>

  				</div>
  			</li>
			<% end -%>
    </ul>
		</div>
		<div role="tabpanel" class="tab-pane" id="carBrand2">
      <ul class="carInfoList clearfix">
      <% @posts_with_brands.values[1].each do |post| %>
  			<li>
  				<div class="logo bfL"><%= image_tag( post.brand.car_photo.try(:image_path) , alt: "")  %></div>
  				<div class="info">
  					<h5 class="tt"><%= link_to post_name(post), post_path(post, {_type: 1}) %></h5>
  					<p class="clearfix"><span class="bfL"></span><%= post.color %><span class="bfR"><%= post.owner_detail %></span></p>
        </li>
       <% end -%>
     </ul>
		</div>
		<div role="tabpanel" class="tab-pane" id="carBrand3">
      <ul class="carInfoList clearfix">
      <% @posts_with_brands.values[2].each do |post| %>
  			<li>
  				<div class="logo bfL"><%= image_tag( post.brand.car_photo.try(:image_path) , alt: "")  %></div>
  				<div class="info">
  					<h5 class="tt"><%= link_to post_name(post), post_path(post, {_type: 1}) %></h5>
  					<p class="clearfix"><span class="bfL"></span><%= post.color %><span class="bfR"><%= post.owner_detail %></span></p>

  				</div>
  			</li>
			<% end -%>
    </ul>
		</div>
		<div role="tabpanel" class="tab-pane" id="carBrand4">
      <ul class="carInfoList clearfix">
      <% @posts_with_brands.values[3].each do |post| %>
  			<li>
  				<div class="logo bfL"><%= image_tag( post.brand.car_photo.try(:image_pat), alt: "")  %></div>
  				<div class="info">
  					<h5 class="tt"><%= link_to post_name(post), post_path(post, {_type: 1}) %></h5>
  					<p class="clearfix"><span class="bfL"></span><%= post.color %><span class="bfR"><%= post.owner_detail %></span></p>

  				</div>
  			</li>
			<% end -%>
    </ul>
		</div>
		<div class="moreLink text-right"><%= link_to '更多 >>', posts_path(_type: 1) %></div>
	</div>
</div>
